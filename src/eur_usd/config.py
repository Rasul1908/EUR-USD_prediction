from __future__ import annotations

# Timestamp column assumptions (adjust once we inspect the raw source format).
TICK_TS_COL = "timestamp"

# Example output "per second" assumptions.
PER_SECOND_TS_COL = "ts_second"

# Example events output name.
EVENTS_TABLE_NAME = "events"

